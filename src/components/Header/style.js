import styled from "styled-components";
import { ContainerStyle } from "../container/style";
export const HeaderContainer = styled(ContainerStyle)`
  display: flex;
  justify-content: flex-end;
  position: absolute;
  top: 0;
  left: 50%;
  transform: translate(-50%, 0);
`;
